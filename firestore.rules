rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: This allows anyone to read/write. Use only for testing.
      allow read, write: if true;
    }
  }
}
